const  numberOfStudent = 20;
const numberOfMentors = 8;
const total = numberOfStudent + numberOfMentors;
console.log(` numero de alumnos  ${numberOfStudent}` );
console.log(` numero de Mentors  ${numberOfMentors}` );

console.log(` total alumnos + Mentors  ${total}` );
