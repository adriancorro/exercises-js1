// Declare your function first

// Call the function and assign to a variable `sum`

function suma (one,two){
    let operacion = one + two;
    return operacion;
}

var sum = suma(13, 124);
console.log(sum);
